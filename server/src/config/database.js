// Database configuration
// This file will contain database connection settings when needed

export const dbConfig = {
  // Database configuration will be added here
  // For now, we're using file-based storage
}
